cat << 'EOF' > README.md
# FlyRank CRUD API

In-memory CRUD Task API implementations.

- **Primary Track**: Python (FastAPI) located in `python-fastapi/`
- **Secondary Track**: JavaScript (Node.js & Express) located in `node-express/`
EOF
